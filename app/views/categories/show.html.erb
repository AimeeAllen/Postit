<% all_posts = link_to('All Posts', posts_path) %>
<% heading = "#{all_posts} &raquo #{@category.name}".html_safe %>

<%= render 'shared/header', header: heading %>

<%= render @posts %>

<%= render 'shared/pageination_links', pages: @post_pages, items_per_page: Post::POSTS_PER_PAGE %>
